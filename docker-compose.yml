services:
  backend:
    build: .
    ports:
      - 8081:8081
    env_file:
      - .env

  postgres:
    image: postgres
    env_file:
      - .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
